#include<stdio.h>
int main(){

    FILE *fptr; //Poniter for File Handling [FILE]
    char data[10]={'W','A','L','E','E','D'};
    fptr=fopen("waleed.txt","r"); //Creating and Opening File [waleed.txt]

    while(fgetc)

    while(fscanf(data,10,fptr)!=NULL)
    fprintf("%c",data); //Storing Data into File [waleed.txt]

return 0;
}